import React from "react";
import {  Route, Routes } from "react-router-dom";

const Routing = () => {
  return (
    <div className="">
      <Routes>
      </Routes>
    </div>
  );
};

export default Routing;
